# Fixture: session — realistic task for the session skill

The user says: "ok, wrap up my session" and gives this state:

> - Finished the CSV export feature; 3 files changed, nothing committed yet.
> - Its beads ticket is done (closed).
> - Remote is reachable; there may be new commits upstream.
> - I want a proper end-of-session wrap-up with commit and push, not a quick
>   stash.

Respond as you would in a live session: name the exact route you would follow
(which mode and which file you would read next), then outline the steps you
would take. This is a simulation — do not execute anything.

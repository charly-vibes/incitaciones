# Fixture: grill-me — realistic task for the grill-me skill

The user pastes this plan and says: "grill me on this":

> We want to add a caching layer to our API service.
> Plan: put Redis in front of the Postgres read path, 5-minute TTL on all
> responses, invalidate the whole cache on every write. Repository context:
> `~/api-service` (this is a simulation — when the protocol calls for
> inspecting the codebase instead of asking, state which path you would
> inspect and what you would look for; do not inspect or implement anything).

Respond as you would in a live session: exactly one turn of the interview.

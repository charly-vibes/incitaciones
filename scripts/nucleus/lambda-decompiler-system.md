λ engage(nucleus).
[phi fractal euler tao pi mu ∃ ∀] | [Δ λ Ω ∞/0 | ε/φ Σ/μ c/h signal/noise order/entropy truth/provability self/other] | OODA
Human ⊗ AI ⊗ REPL

λ bridge(x). lambda → prose | structural_equivalence
| preserve(semantics) | analyze(¬execute)
| decompile: lambda → prose_markdown

Decompile lambda notation into concise markdown prose.
Preserve all executable instructions, mandatory constraints, negative constraints, output-format requirements, and logical hierarchy.
Output prose markdown only. No lambda notation. No code fences. No commentary.

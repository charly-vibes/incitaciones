# Moved: issue-review

This skill was consolidated into the **issues** skill (router + references/ layout).

Use `/issue-review` no more: invoke `/skill:issues` and follow its mode table — this task is the **issue-review** mode, which reads `references/issue-review/SKILL.md`.

This pointer is a permanent compiled alias (incitaciones-06i): every distribution surface renders it as banner + full member content. It is never deleted; old URLs and /skill invocations keep working forever.

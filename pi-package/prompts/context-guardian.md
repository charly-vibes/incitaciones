---
description: "Shortcut for the context-guardian skill. System prompt that enforces reuse of existing abstractions and prevents architectural drift during development"
---
<!-- installed-from: incitaciones -->

Use the installed `context-guardian` skill for this task.

If the skill is not automatically loaded, invoke `/skill:context-guardian` and follow it.

User context: $@

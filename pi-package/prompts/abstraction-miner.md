---
description: "Shortcut for the abstraction-miner skill. Scan codebase for semantic duplication and propose reusable abstractions — advisory only, no code changes"
---
<!-- installed-from: incitaciones -->

Use the installed `abstraction-miner` skill for this task.

If the skill is not automatically loaded, invoke `/skill:abstraction-miner` and follow it.

User context: $@

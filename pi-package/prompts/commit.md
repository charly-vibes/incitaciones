---
description: "Shortcut for the commit skill. Create well-structured, atomic git commits with clear intent"
---
<!-- installed-from: incitaciones -->

Use the installed `commit` skill for this task.

If the skill is not automatically loaded, invoke `/skill:commit` and follow it.

User context: $@

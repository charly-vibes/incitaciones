---
description: "Shortcut for the test-abstraction-miner skill. Detect Lazy Test clusters in test suites and propose parameterized tests and property-based test escalations — advisory only, no code changes"
---
<!-- installed-from: incitaciones -->

Use the installed `test-abstraction-miner` skill for this task.

If the skill is not automatically loaded, invoke `/skill:test-abstraction-miner` and follow it.

User context: $@
